let age = 26;
let dogYears = (age + 1) / 7;

console.log(dogYears);

// Challenge

let studentScore = 18;
let maxScore = 20;
let percentage = (studentScore / maxScore) * 100;

console.log(`${studentScore}/${maxScore} -> You got a ${percentage}%!`);

studentScore = 1640;
maxScore = 2000;
percentage = (studentScore / maxScore) * 100;

console.log(`${studentScore}/${maxScore} -> You got a ${percentage}%!`);
